import { Keyboard } from 'vk-io'

const cityMarkup = [
        Keyboard.textButton({
            label: 'Москва',
            color: 'default',
        }),
        Keyboard.textButton({
            label: 'Санкт-Петербург',
            color: 'default',

        })
]

export default cityMarkup